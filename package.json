{
  "name": "server_NodeJS+TS",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/**/*.json dist/",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
  },
  "private": true,
  "dependencies": {
    "uuid": "^11.1.0"
  }
}
